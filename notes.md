# Day27 Git Revert

## Task:-
Navigate to the Git repository located at /usr/src/kodekloudrepos/cluster and revert the latest commit (HEAD) to the previous commit (the one containing the initial commit message). Create a new revert commit using the message "revert cluster".

## Purpose:-
This task safely undoes the changes introduced by the most recent commit without rewriting repository history. Using git revert creates a new commit that reverses the changes from HEAD while preserving commit integrity. Providing the specifie

